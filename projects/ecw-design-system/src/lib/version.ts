/**
 * The current version of the @ecw/design-system library.
 *
 * This is a placeholder surface for the setup Jira (PE-314767) so the
 * publishable package has a real, typed export and the library build
 * produces a meaningful bundle before any production components exist.
 */
export const DESIGN_SYSTEM_VERSION = '0.0.1';
