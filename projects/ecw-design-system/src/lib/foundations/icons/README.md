# Icon System

Icon system — implemented in PE-315893 (Material icons + custom SVG fallback). Will document icon sizing conventions, usage guidelines, and any icon-specific tokens (e.g. `--ecw-icon-size-*`) that integrate with the token layers defined in `../tokens/`.
