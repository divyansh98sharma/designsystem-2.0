# Typography Foundation

Typography foundation — implemented in PE-315736. Will document the type scale, font weights, line-heights, and letter-spacing decisions as Storybook docs and expose them as typography tokens following the `--ecw-font-*` and `--ecw-line-height-*` naming conventions established in `../tokens/primitives.css`.
